/**
 * Stub temporário — implementação real vem na Task 6 deste PR.
 */
export function CampaignCharactersList(/* { campaignId, onOpen } */) {
  return <div className="p-4 text-gray-500 text-sm">Fichas da mesa: (em construção)</div>
}
